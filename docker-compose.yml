services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/queueflow.db
      CORS_ORIGINS: http://localhost:8000
      FRONTEND_URL: http://localhost:8000
    volumes:
      - queueflow_data:/data

volumes:
  queueflow_data:

